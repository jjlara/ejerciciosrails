<%= form_for(@ejercicio) do |f| %>
  <% if @ejercicio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ejercicio.errors.count, "error") %> prohibited this ejercicio from being saved:</h2>

      <ul>
      <% @ejercicio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <%= f.fields_for :pregunta do |p| %>
  <div class="field">
    <%= p.label :titulo %><br>
    <%= p.text_field :titulo %>
  </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
